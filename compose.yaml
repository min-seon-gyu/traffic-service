services:
  mysql:
    image: mysql:8.0
    ports:
      - "13306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: trafficdb
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
